# Plan: Fix & Unify Repository Documentation

**Date**: 2026-02-09 17:27  
**Scope**: All 28 markdown files across the repository  
**Goal**: Fix broken links, factual errors, naming inconsistencies, and documentation gaps to make all docs coherent across the 4 scenarios

## TL;DR

An audit of the repository found **7 broken links**, **9+ factual errors**, **~15 naming/structure inconsistencies**, and significant documentation gaps — particularly around scenario 4's evolved feature set (dual models, NIM integration, podcast assets). The root cause is incremental feature additions without updating earlier docs. This plan fixes every issue systematically across 8 phases.

## Steps

### Phase 1: Fix broken links (7 files)

1. In `scenario4/AppHost/README.md` — change `../AZURE_DEPLOYMENT.md` → `../docs/AZURE_DEPLOYMENT.md`
2. In `scenario4/docs/APPINSIGHTS_QUICKSTART.md` — change `../AZURE_DEPLOYMENT.md` → `AZURE_DEPLOYMENT.md` (same directory)
3. In `scenario4/docs/STRUCTURE.md` — change `[README.md](README.md)` → `[README.md](../README.md)`
4. In `scenario4/docs/TESTING_CHECKLIST.md` — change `[README.md](README.md)` → `[README.md](../README.md)` and `[TROUBLESHOOTING.md](README.md#troubleshooting)` → `[Troubleshooting](../README.md#troubleshooting)`
5. In `scenario4/docs/QUICKREF.md` — change `scenario4/AZURE_DEPLOYMENT.md` → `scenario4/docs/AZURE_DEPLOYMENT.md`
6. In `scenario4/docs/USAGE_EXAMPLES.md` — fix two links at the bottom: `../AZURE_DEPLOYMENT.md` → `AZURE_DEPLOYMENT.md` and `../QUICKREF.md` → `QUICKREF.md`

### Phase 2: Fix factual errors

7. In `docs/IMPLEMENTATION_SUMMARY.md` — replace all old filenames: `scenario1_simple.py` → `scenario1/transcribe.py`, `scenario2_interactive.py` → `scenario2/transcribe.py`, `scenario3_multilingual.py` → `scenario3/transcribe.py`. Update all corresponding usage commands. Add a note at top that this covers the v2.0 implementation (scenarios 1–3 only).
8. In `docs/PLAN.md` — same filename replacements. Add header note clarifying this was the original plan for scenarios 1–3.
9. In `docs/USAGE_EXAMPLES.md` — change `scenario3_multilingual.py` → `scenario3/transcribe.py` in the troubleshooting section.
10. In `scenario4/clients/console/README.md` — change ".NET 8.0 SDK" → ".NET 10.0 SDK", change `net8.0` → `net10.0` in the build output path, and document the missing CLI flags: `--async`, `--model`, `--language`, `--no-timestamps`, `--generate-assets`, `--transcript-file`, `--nim-url`.
11. In `scenario4/AppHost/README.md` — change `name: "apiendpoint"` → `name: "http"` in the code snippet to match actual `AppHost/Program.cs`.
12. In `scenario4/docs/ARCHITECTURE.md` — update "Main application (210 lines)" to reflect actual line count (~500+ lines).
13. In `scenario4/server/README.md` — update title/description to mention dual model support (Parakeet + Canary), add `model`, `language`, `include_timestamps` parameters to the `/transcribe` POST docs, fix the `MODEL_NAME` env var reference.

### Phase 3: Update scenario 3 documentation for multi-model support

14. In `scenario3/README.md` — verify the model table is accurate and matches the code's `AVAILABLE_MODELS` dict. Ensure default model (`canary-1b-v2`) is called out correctly.

### Phase 4: Update scenario 4 structural documentation

15. In `scenario4/docs/STRUCTURE.md` — add missing files: `TranscriptionTelemetry.cs` under `ServiceDefaults/`, `nvidia_asr_monitor.py` and `requirements-windows.txt` under `server/`, `PodcastAssets.razor` under `Components/Pages/`, `NimPodcastService.cs` under `Services/`, `test_server.py` and `Directory.Build.props` in scenario4 root. Update "NVIDIA Parakeet (English)" to "NVIDIA Parakeet + Canary (English + Multilingual)".
16. In `scenario4/clients/webapp/README.md` — add `NimPodcastService.cs`, `PodcastAssets.razor`, and layout CSS files to the project structure listing. Add a section documenting NIM podcast asset generation features.

### Phase 5: Update root-level docs to cover all 4 scenarios

17. In `docs/QUICKREF.md` — add a Scenario 4 section with key commands (server start, Docker, Aspire, console client, webapp).
18. In `README.md` (root) — add mention of NIM integration and podcast asset generation under the scenario 4 description. Clarify that the Blazor web app is in the `clients/webapp/` folder.

### Phase 6: Update `.github/copilot-instructions.md`

19. Change "offers three scenarios" → "offers four scenarios".
20. Add scenario 4 row to the Architecture & Scenarios table: `scenario4/` | Parakeet + Canary | Client-server architecture with REST API, .NET Aspire orchestration.
21. Change `clients/blazor/` → `clients/webapp/` in the structure tree.
22. Expand the scenario4 structure tree to include `AppHost/`, `ServiceDefaults/`, `test_server.py`, `Directory.Build.props`, `NvidiaTranscribe.slnx`, `nvidia_asr_monitor.py`, `setup-venv.ps1/.sh`, `requirements-windows.txt`, and all 14 docs files.
23. Add `labs/` and `scenario1/simple-trasnscribe.py` to the root structure tree.
24. Update the Model Constants section: change scenario 3's `MODEL_NAME` from `"nvidia/canary-1b"` to `"nvidia/canary-1b-v2"` (default) and note it supports 3 models.

### Phase 7: Fix filename typo

25. Rename `scenario1/simple-trasnscribe.py` → `scenario1/simple-transcribe.py` (fixing "trasnscribe" typo).

### Phase 8: Minor cleanup

26. In `scenario4/docs/GPU_SETUP_GUIDE.md` — add a note clarifying the "before" code snippet is historical context and the fix has already been applied.
27. In `docs/plans/plan_260209_1008.md` — add a header note that the actual implementation diverged from the plan (uses `meta/llama-3.2-3b-instruct` instead of `nvidia/llama-3.2-nv-minitron-4b-instruct`).

## Verification

- Grep all `.md` files for relative links matching `](../` and `](` patterns, then verify each target file exists at the resolved path
- Run `grep -rn "scenario1_simple\|scenario2_interactive\|scenario3_multilingual" docs/` to confirm no old filenames remain
- Run `grep -rn "blazor/" .github/ README.md` to confirm `blazor/` folder references are removed
- Run `grep -rn "net8.0" scenario4/` to confirm no stale .NET 8 references remain
- Run `grep -rn "three scenarios" .github/` to confirm the count is updated
- Manually verify each fixed link by checking the target file exists

## Decisions

- **Rename `simple-trasnscribe.py`**: Fixing the typo since it's a maintenance file — existing users are unlikely to depend on the misspelled name.
- **Root docs (`PLAN.md`, `IMPLEMENTATION_SUMMARY.md`) scope**: Rather than heavily reworking these to add scenario 4, add a header note clarifying they cover the v2.0 (scenarios 1–3) implementation. Scenario 4 has its own comprehensive docs in `scenario4/docs/`. The only root doc that should add scenario 4 content is `QUICKREF.md` (since it's a reference users actively consult).
- **`labs/` folder**: Document it in the structure tree as an empty folder for experiments, since it exists in the repo.
- **"Blazor" naming**: Keep "Blazor" as a descriptive term (the webapp *is* a Blazor app) but clarify that the folder is `clients/webapp/`, not `clients/blazor/`.

## Issues Summary

| Category | Count |
|----------|-------|
| Broken links | 7 |
| Factual errors | 9 |
| Naming inconsistencies | 7 |
| Missing documentation entries | 11 |
| Filename typos | 1 |
| Files needing update | 15 |
