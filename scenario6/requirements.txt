# Scenario 6 — PersonaPlex Real-Time Voice Conversation
# Install PyTorch with CUDA first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Then install the moshi package from the PersonaPlex repo:
#   git clone https://github.com/NVIDIA/personaplex.git /tmp/personaplex
#   pip install /tmp/personaplex/moshi/.
# Then install these remaining dependencies:

fastapi
uvicorn[standard]
python-dotenv
aiohttp>=3.10.5,<3.11
huggingface-hub>=0.24
websockets
